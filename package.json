{
  "name": "libs",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage --ui",
    "ci:build": "pnpm run -r ci:build",
    "ci:test": "vitest run --coverage",
    "ci:api-extractor": "pnpm run -r api:extractor",
    "publish:test": "pnpm publish --no-git-checks --dry-run -r"
  },
  "devDependencies": {
    "@eavid/lib-dev": "^0.0.3",
    "@microsoft/api-extractor": "^7.39.0",
    "@types/node": "^20.10.5",
    "@vitest/coverage-v8": "^0.33.0",
    "@vitest/ui": "^1.1.0",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  }
}
